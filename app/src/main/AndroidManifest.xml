<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Superandroidmaster">
        <activity
            android:name=".sintaxis.todoapp.TodoActivity"
            android:theme="@style/Theme.TODOApp"
            android:exported="false" />
        <activity
            android:name=".sintaxis.imccalucalator.ResultIMCActivity"
            android:exported="false" />
        <activity
            android:name=".sintaxis.imccalucalator.imcCalculatorActivity"
            android:exported="false" />
        <activity
            android:name=".sintaxis.firstapp.MenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".sintaxis.firstapp.ResultActivity"
            android:exported="false" />
        <activity
            android:name=".sintaxis.firstapp.FirstAppActivity"
            android:exported="false" />
    </application>

</manifest>